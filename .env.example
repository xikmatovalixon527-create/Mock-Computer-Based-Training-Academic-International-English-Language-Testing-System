# Database connection string to your Neon serverless PostgreSQL instance
DATABASE_URL=postgres://user:password@hostname/database?sslmode=require

# Private secret key used to sign and verify session JWTs (jose-based)
JWT_SECRET=your_super_secret_jwt_key_here
